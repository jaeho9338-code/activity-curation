# Supabase 연결용. 실제 값은 .env에 넣는다(.env는 커밋 금지).
# Supabase 프로젝트 > Settings > API 에서 복사.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 자연어 추천 API 주소. 로컬 개발은 안 넣어도 됨(기본 http://localhost:8787/api/recommend).
# 배포 때만 서버리스 함수 주소를 넣는다. 예: /api/recommend
# VITE_RECOMMEND_API=
