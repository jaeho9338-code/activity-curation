# 2주차 주간 계획

금요일이 공휴일이라 수·목 이틀로 압축한다. 이번 주 목표는 화면을 만들고(완료), **API 소스 하나로 진짜 데이터를
Supabase에 넣어 화면에 띄우는 것(실데이터 한 바퀴)**까지다. 크롤링과 자유 문장 LLM 파싱은 목 하루엔 무리라
다음 주로 넘긴다. 제일 어려운 크롤링·LLM은 3주차에 제대로 찌른다.

## 요일별

### 월 - 계획·이슈·문서 (완료)
주간 계획, GitHub 이슈, 기획·데이터 전략 정리.

### 화 - React 핵심 화면 (완료)
조건 입력 -> 가능/확인 필요/거의 가능 분류. 검색·필터·즐겨찾기·정렬·상세 모달·컴포넌트 분리.

### 수 - 화면 흐름(라우팅) + 데이터 모델 설계 (완료)
react-router로 홈·공고 상세·즐겨찾기 페이지 분리, 상단 네비. 데이터 모델은 **Supabase에서 프론트가 직접
조회**하는 걸로 결정(Express 조회 서버 안 만듦, 매칭은 클라 match.js). 내일 만들 테이블 SQL(raw_postings,
postings)까지 확정.

### 목 - Supabase 실데이터 한 바퀴 (금요일 공휴일이라 여기서 마무리)
1. Supabase 프로젝트 + 테이블(raw_postings, postings) 생성. 올리기 전 security-check로 키 검사.
2. API 소스 하나(온통청년 또는 링커리어 내부 API)에서 실데이터를 받아 postings에 저장(Node 스크립트).
3. 화면을 mock -> supabase-js 직접 조회(useEffect)로 교체. 로딩·에러 처리. eligibility 모양이 같아 match.js는 그대로.
4. 진짜 공고가 화면에 뜨면 이번 주 성공.

### 금 - 공휴일

## 다음 주로 넘기는 것 (3주차)
- 콘테스트코리아 크롤링(총망라), 자유 문장 LLM 파싱, 소스 간 중복 제거.
- 정확도 측정(gold 라벨링).

## 규칙
- 커밋 작게. 올리기 전 security-check로 키·개인정보 검사.
- main에 절대 안 올린다. 오직 N186_이재호 브랜치.
- 비용은 신규만 파싱, 온디맨드. 원문은 재게시 없이 링크로.
